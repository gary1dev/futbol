[build]
  publish = "frontend"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[functions]
  timeout = 26

# Redirige /api/datos → la Function JS
[[redirects]]
  from = "/api/datos"
  to   = "/.netlify/functions/datos"
  status = 200

# SPA fallback
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
